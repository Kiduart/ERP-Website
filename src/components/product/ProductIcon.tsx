import {
  Activity,
  BarChart3,
  BellRing,
  BookMarked,
  BookOpen,
  BriefcaseBusiness,
  Building,
  Building2,
  Bus,
  CalendarCheck,
  CalendarRange,
  ClipboardCheck,
  Compass,
  CreditCard,
  Database,
  FileSpreadsheet,
  Fingerprint,
  GraduationCap,
  HeartHandshake,
  KeyRound,
  Layers,
  LayoutDashboard,
  Library,
  LifeBuoy,
  LineChart,
  Megaphone,
  MessageSquare,
  MonitorSmartphone,
  Network,
  PieChart,
  ScrollText,
  ServerCog,
  ShieldAlert,
  ShieldCheck,
  Smartphone,
  TrendingDown,
  UserPlus,
  Users,
  Wallet,
  Warehouse,
} from "lucide-react";

/** Explicit map keeps the icon set tree-shakeable while data files stay plain strings. */
const ICONS = {
  Activity,
  BarChart3,
  BellRing,
  BookMarked,
  BookOpen,
  BriefcaseBusiness,
  Building,
  Building2,
  Bus,
  CalendarCheck,
  CalendarRange,
  ClipboardCheck,
  Compass,
  CreditCard,
  Database,
  FileSpreadsheet,
  Fingerprint,
  GraduationCap,
  HeartHandshake,
  KeyRound,
  Layers,
  LayoutDashboard,
  Library,
  LifeBuoy,
  LineChart,
  Megaphone,
  MessageSquare,
  MonitorSmartphone,
  Network,
  PieChart,
  ScrollText,
  ServerCog,
  ShieldAlert,
  ShieldCheck,
  Smartphone,
  TrendingDown,
  UserPlus,
  Users,
  Wallet,
  Warehouse,
} as const;

export type ProductIconName = keyof typeof ICONS;

export function ProductIcon({ name, className }: { name: string; className?: string }) {
  const Icon = ICONS[name as ProductIconName] ?? LayoutDashboard;
  return <Icon className={className} aria-hidden="true" />;
}
